
<div class="row" style="margin-top:20px;">

    <div class="col s12 m4">
      <div class="card-panel dashboard-panel style-0">
        <p>The GrowHaus works to provide fresh and healthy food at a low price to local residents and the greater Denver community.</p>
        <%= link_to forms_path(department_id:@departments.select{|d|d.name=="Distribution"}.first.id) do %>
          Distribution &gt;
        <% end %>
      </div>
    </div>
    <div class="col s12 m4">
      <div class="card-panel dashboard-panel style-1">
        <p>Our growing operations demonstrate a variety of innovative, sustainable, and community-oriented urban farming techniques.</p>
        <%= link_to forms_path(department_id:@departments.select{|d|d.name=="Production"}.first.id) do %>
          Production &gt;
        <% end %>
      </div>
    </div>
    <div class="col s12 m4">
      <div class="card-panel dashboard-panel style-2">
        <p>The GrowHaus aims to be a hub of learning about nutrition, gardening, cooking, and other skills for building healthy communities.</p>
        <%= link_to forms_path(department_id:@departments.select{|d|d.name=="Education"}.first.id) do %>
          Education &gt;
        <% end %>
      </div>
    </div>
</div>

<div class="row">
  <div class="col s12">
    <div class="card-panel">
      <h3 class="dashboard-activity-head">Revenue Summary</h3>
      <div class="dashboard-activity-subhead">per Department</div>
      <div id="revenue-summary-chart" style="width: 100%; height: 300px;"></div>
    </div>
  </div>
</div>

<style type="text/css">
  html { 
      height: 100%;
      margin:0px;
      background: url(<%=image_path("/GrowHaus-BKG2.jpg")%>) no-repeat center center;
      background-size: cover;
      background-attachment: fixed;
  }
</style>

<script type="text/javascript">
  google.load("visualization", "1.1", {packages:["bar"]});
  google.setOnLoadCallback(drawChart);
  function drawChart() {
    var data = google.visualization.arrayToDataTable(
      <%=raw @revenue_chart_data.to_s%>
    );

    var options = {
      bars: 'vertical'
    };

    var chart = new google.charts.Bar(document.getElementById('revenue-summary-chart'));

    chart.draw(data, options);
  }
</script>
